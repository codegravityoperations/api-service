package com.codegravity.itconsultancy.enums;

/**
 * Distinguishes which type of user is being registered.
 * Used in MailLog and throughout the service layer.
 */
public enum UserType {
    EMPLOYEE,
    CANDIDATE
}
